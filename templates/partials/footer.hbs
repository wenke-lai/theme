<footer class="border-t border-border bg-background">
  <div class="container mx-auto flex flex-col items-center justify-between gap-4 py-10 md:h-24 md:flex-row md:py-0">
    <div class="flex flex-col items-center gap-4 px-8 md:flex-row md:gap-2 md:px-0">
      <div class="flex items-center space-x-2">
        {{#if @site.icon}}
          <img src="{{@site.icon}}" alt="{{@site.title}}" class="h-5 w-5" />
        {{/if}}
        <p class="text-center text-sm leading-loose text-muted-foreground md:text-left">
          {{@site.title}}
        </p>
      </div>
    </div>
    <div class="flex items-center space-x-4">
      <nav class="flex items-center space-x-4 text-sm">
        <a class="text-muted-foreground hover:text-foreground transition-colors" href="{{@site.url}}/privacy/">Privacy</a>
        <a class="text-muted-foreground hover:text-foreground transition-colors" href="{{@site.url}}/terms/">Terms</a>
        <a class="text-muted-foreground hover:text-foreground transition-colors" href="{{@site.url}}/contact/">Contact</a>
      </nav>
    </div>
  </div>
  <div class="border-t border-border">
    <div class="container mx-auto flex flex-col items-center justify-between gap-4 py-6 md:flex-row">
      <p class="text-center text-sm text-muted-foreground md:text-left">
        © {{date format="YYYY"}} {{@site.title}}. All rights reserved.
      </p>
      <p class="text-center text-sm text-muted-foreground md:text-right">
        {{@site.description}}
      </p>
    </div>
  </div>
</footer>